# Examples

- `basic_usage.py`: run the portable assignment pipeline on configured inputs
- `batch_processing.py`: command template for a full Parquet run
- `validation_example.py`: generate validation JSON

Update the example path constants before running them.
